<div class="span8">
  <p><%= link_to image_tag("gold_star.png"), star_path(student_star) %></p>
  <p>
    <% @user.achievements.where("message IS NOT NULL").each do |achievement| %>
      <%= User.find_by_id(achievement.sender_id).name %>: <%= achievement.message %>
    <% end %>
  </p>
</div>
