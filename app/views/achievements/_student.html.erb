<div class="star">
  <% Achievement.student_message(@user).each do |achievement| %>
    <%= image_tag("gold_star.png", "data-toggle" => "tooltip", :title => achievement.message ) %>
  <% end %>
</div>

