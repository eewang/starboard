<% content_for :section_title do %>
  <%= @user.name %>
<% end %>

<% content_for :sidebar_profile_pic do %>
  <%= image_tag @user.safe_profile_pic %>
<% end %>

<h1>Editing user</h1>

    <%= form_for(@user) do |f| %>
      <%= f.label :is_teacher %>
      <%= f.check_box :is_teacher %>
      <%= f.submit 'Submit' %>
    <% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
