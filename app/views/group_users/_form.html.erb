<%= form_for(@group_user) do |f| %>
  <% if @group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_user.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <p>Group Name</p>
  <%= text_field_tag 'group_name' %>
  </div>

   <div>
  <p>Group Password</p>
  <%= text_field_tag 'group_password' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
